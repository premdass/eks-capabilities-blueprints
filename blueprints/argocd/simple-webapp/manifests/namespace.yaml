# Namespace for the simple-webapp application
# This creates an isolated namespace for all application resources
apiVersion: v1
kind: Namespace
metadata:
  # Name of the namespace - all application resources will be deployed here
  name: simple-webapp
  labels:
    # Labels for identification and filtering
    app.kubernetes.io/name: simple-webapp
    app.kubernetes.io/managed-by: argocd
